%h1 Listing users

%table#table
  %thead
    %tr
      %th Name
      %th Email
      %th Password digest
      %th
      %th
      %th

  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
        %td= user.email
        %td= user.password_digest
        %td= link_to 'Show', user
        %td= link_to 'Edit', edit_user_path(user)
        %td= link_to 'Destroy', destroy_user_path(user), method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New User', new_user_path